<footer class="site-footer">
  <div class="footer-container">
    <div class="footer-branding">
      {% if section.settings.footer_logo %}
        <img src="{{ section.settings.footer_logo | img_url: 'medium' }}" alt="Logo" class="footer-logo" />
      {% endif %}
      <p>{{ section.settings.footer_text }}</p>
    </div>

    <div class="footer-links">
      {% for block in section.blocks %}
        {% if block.settings.icon != blank %}
          <a href="{{ block.settings.link }}" class="footer-link" target="_blank">
            <div class="footer-card {{ block.settings.label | downcase }}">
              <img src="{{ block.settings.icon | img_url: '50x50' }}" alt="{{ block.settings.label }}" />
              <span>{{ block.settings.label }}</span>
            </div>
          </a>
        {% endif %}
      {% endfor %}
    </div>
  </div>

  <div class="footer-bottom">
    <p>© {{ 'now' | date: "%Y" }} {{ shop.name }}. Todos los derechos reservados.</p>
  </div>
</footer>

<style>
.site-footer {
  background-color: #001F3F;
  color: white;
  padding: 40px 20px 20px;
  font-size: 14px;
  text-align: center;
}

.footer-container {
  display: flex;
  flex-direction: column;
  gap: 30px;
  max-width: 1200px;
  margin: 0 auto;
  align-items: center;
}

.footer-branding {
  max-width: 400px;
}

.footer-logo {
  max-height: 60px;
  margin-bottom: 10px;
}

.footer-links {
  display: flex;
  gap: 25px;
  flex-wrap: wrap;
  justify-content: center;
}

.footer-card {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-decoration: none;
  gap: 10px;
  color: white;
  font-weight: 500;
  font-size: 14px;
  transition: transform 0.3s ease;
}

.footer-card:hover {
  transform: translateY(-3px);
}

.footer-card img {
  width: 50px;
  height: 50px;
  padding: 10px;
  background-color: white;
  border-radius: 50%;
  object-fit: contain;
  box-shadow: 0 4px 8px rgba(0,0,0,0.2);
}

/* Estilos opcionales por red social */
.footer-card.facebook img {
  background-color: #1877F2;
}

.footer-card.instagram img {
  background: radial-gradient(circle at 30% 30%, #feda75, #d62976, #962fbf, #4f5bd5);
}

.footer-card.whatsapp img {
  background-color: #25D366;
}

.footer-bottom {
  border-top: 1px solid rgba(255, 255, 255, 0.2);
  margin-top: 30px;
  padding-top: 10px;
}
</style>

{% schema %}
{
  "name": "Pie de página",
  "settings": [
    {
      "type": "image_picker",
      "id": "footer_logo",
      "label": "Logo del footer"
    },
    {
      "type": "text",
      "id": "footer_text",
      "label": "Texto del footer",
      "default": "Gracias por visitar nuestro sitio web."
    }
  ],
  "blocks": [
    {
      "type": "social_link",
      "name": "Enlace de red social",
      "settings": [
        {
          "type": "image_picker",
          "id": "icon",
          "label": "Ícono"
        },
        {
          "type": "text",
          "id": "label",
          "label": "Etiqueta",
          "default": "Red social"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Enlace"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Footer Profesional"
    }
  ]
}
{% endschema %}
