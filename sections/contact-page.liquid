<div class="contact-wrapper">
  <div class="contact-grid">
    <div class="contact-info">
      <h2>{{ section.settings.heading }}</h2>
      <p>{{ section.settings.subtext }}</p>
      <ul class="info-list">
        <li><strong>📞 Teléfono:</strong> {{ section.settings.phone }}</li>
        <li><strong>✉️ Email:</strong> <a href="mailto:{{ section.settings.email }}">{{ section.settings.email }}</a></li>
        <li><strong>📍 Dirección:</strong> {{ section.settings.address }}</li>
      </ul>
      <div class="contact-map">
        {{ section.settings.map_embed_code }}
      </div>
    </div>
    <div class="contact-form">
      <form method="post" action="/contact#contact_form" id="contact_form" accept-charset="UTF-8" class="form">
        <input type="hidden" name="form_type" value="contact" />
        <input type="hidden" name="utf8" value="✓" />
        <input type="text" name="contact[name]" placeholder="Tu nombre" required />
        <input type="email" name="contact[email]" placeholder="Tu correo" required />
        <textarea name="contact[body]" rows="5" placeholder="Escribe tu mensaje" required></textarea>
        <button type="submit">Enviar</button>
      </form>
    </div>
  </div>
</div>

<style>
.contact-wrapper {
  padding: 60px 20px;
  max-width: 1200px;
  margin: auto;
  background: #fff;
  border-radius: 12px;
}

.contact-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 40px;
}

.contact-info h2 {
  font-size: 28px;
  margin-bottom: 10px;
}

.contact-info p {
  font-size: 16px;
  margin-bottom: 20px;
}

.info-list {
  list-style: none;
  padding: 0;
  margin: 0 0 20px;
}

.info-list li {
  margin-bottom: 10px;
  font-size: 15px;
}

.contact-form input,
.contact-form textarea {
  width: 100%;
  margin-bottom: 15px;
  padding: 12px;
  border: 1px solid #ddd;
  border-radius: 6px;
  font-size: 15px;
}

.contact-form button {
  background-color: #002855;
  color: white;
  border: none;
  padding: 12px 24px;
  font-size: 15px;
  border-radius: 6px;
  cursor: pointer;
  transition: 0.3s ease;
}

.contact-form button:hover {
  background-color: #00509e;
}

.contact-map iframe {
  border: 0;
  width: 100%;
  height: 250px;
  border-radius: 8px;
}
</style>

{% schema %}
{
  "name": "Contacto",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Título",
      "default": "Contáctanos"
    },
    {
      "type": "text",
      "id": "subtext",
      "label": "Texto secundario",
      "default": "¿Tienes dudas? Llámanos o escríbenos."
    },
    {
      "type": "text",
      "id": "phone",
      "label": "Teléfono",
      "default": "+52 871 123 4567"
    },
    {
      "type": "text",
      "id": "email",
      "label": "Email",
      "default": "info@sewingsolution.com.mx"
    },
    {
      "type": "text",
      "id": "address",
      "label": "Dirección",
      "default": "Calle Ejemplo 123, Torreón, Coahuila"
    },
    {
      "type": "textarea",
      "id": "map_embed_code",
      "label": "Código de mapa (iframe)",
      "default": "<iframe src='https://www.google.com/maps/embed?...' width='100%' height='300' style='border:0;' allowfullscreen='' loading='lazy'></iframe>"
    }
  ],
  "presets": [
    {
      "name": "Contacto"
    }
  ]
}
{% endschema %}
