<div class="volver-al-catalogo">
  <a href="/pages/tienda">&larr; Volver al catálogo</a>
</div>

<div class="producto-detalle">
  <h1>{{ product.title }}</h1>

  <p class="precio">{{ product.price | money }}</p>

  <form method="post" action="/cart/add">
    <input type="hidden" name="id" value="{{ product.variants.first.id }}">
    <button type="submit" class="btn-agregar">Agregar al carrito</button>
  </form>

  <div class="descripcion">
    {{ product.description }}
  </div>

  <div class="especificaciones">
    <h2>ESPECIFICACIONES</h2>
    <ul>
      <li><strong>Modelo Compatible:</strong> {{ product.title }}</li>
      <li><strong>SKU:</strong> {{ product.sku }}</li>
      <li><strong>Marca:</strong> {{ product.vendor }}</li>
      <li><strong>Calidad:</strong> 
        {% assign calidad = product.tags | join: ", " | split: ", " | first %}
        {{ calidad }}
      </li>
    </ul>
  </div>
</div>

<style>
.volver-al-catalogo {
  margin-bottom: 20px;
}
.volver-al-catalogo a {
  display: inline-block;
  color: #0056b3;
  font-weight: 600;
  text-decoration: none;
  font-size: 14px;
}
.volver-al-catalogo a:hover {
  text-decoration: underline;
}
.producto-detalle {
  max-width: 700px;
  margin: auto;
  padding: 30px 15px;
}
.precio {
  font-size: 24px;
  color: #007bff;
  font-weight: bold;
}
.btn-agregar {
  margin-top: 10px;
  padding: 10px 20px;
  background-color: #0056b3;
  color: white;
  border: none;
  cursor: pointer;
  border-radius: 6px;
}
.btn-agregar:hover {
  background-color: #003e80;
}
.especificaciones {
  margin-top: 30px;
  background: #f6f6f6;
  padding: 20px;
  border-radius: 8px;
}
.especificaciones h2 {
  margin-bottom: 10px;
  font-weight: bold;
}
.especificaciones ul {
  list-style: none;
  padding: 0;
}
.especificaciones li {
  margin-bottom: 8px;
}
</style>
